# Required — GitHub personal access token with `repo` + `read:discussion` scopes
GITHUB_TOKEN=

# Optional overrides (defaults shown below)
# SURREAL_URL=http://localhost:8018
# SURREAL_NS=githubembed
# SURREAL_DB=main
# OLLAMA_URL=http://localhost:11434
# OLLAMA_EMBED_MODEL=nomic-embed-text
